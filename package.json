{
  "name": "telegram-bot-docs-skill",
  "version": "0.1.0-beta.2",
  "private": true,
  "description": "Agent Skill for freshness-checked Telegram Bot Platform documentation",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check scripts/refresh.mjs && node --check scripts/smoke-install.mjs",
    "test": "npm run test:offline",
    "test:offline": "node --test tests/refresh.test.mjs tests/metadata.test.mjs",
    "test:live": "node --test tests/live.test.mjs",
    "test:all": "npm run test:offline && npm run test:live",
    "smoke:install": "node scripts/smoke-install.mjs"
  }
}
